import './header.css'

const Header = () => {
    return (
        <nav className="navbar">
            <div className="container-fluid">
                <span className="navbar-brand mb-0 h1">URLShortener</span>
            </div>
        </nav>
    );
};

export default Header;